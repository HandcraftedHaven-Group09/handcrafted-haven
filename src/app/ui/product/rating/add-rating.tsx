export default function AddRating({
  productId,
  userId,
}: {
  productId: number;
  userId: number;
}) {
  console.log('IN ADD RATING');
  return <div className="existing-ratings">UNDER CONSTRUCTION</div>;
}
